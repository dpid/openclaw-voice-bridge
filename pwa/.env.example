# Moltbot Voice Bridge - PWA Configuration
# Copy to .env and fill in values (or set at build time)

# =============================================================================
# Proxy Connection
# =============================================================================

# WebSocket URL for the proxy server
# Default: ws://localhost:3001/ws (for local development)
VITE_PROXY_URL=wss://your-proxy-server.example.com/ws
